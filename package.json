{
  "name": "arkntools-packages-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "typescript": "^5"
    }
  },
  "scripts": {
    "update-lock": "bun update --lockfile-only"
  }
}
